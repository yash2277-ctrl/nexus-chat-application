{
  "name": "nexus-chat",
  "version": "1.0.0",
  "description": "Nexus Chat - Next-gen encrypted messaging platform",
  "private": true,
  "scripts": {
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "dev": "npm run client",
    "install:all": "npm install && cd client && npm install",
    "build": "npm install --prefix client && npm run build --prefix client",
    "start": "npm install --prefix client && npm run build --prefix client && node server/index.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.2",
    "socket.io": "^4.7.4",
    "uuid": "^9.0.1"
  }
}
